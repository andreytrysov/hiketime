# hiketime — прототип 1

Модель времени прохождения пешего маршрута по рельефу и весу рюкзака.
Без интерфейса: цель — проверить, попадает ли модель в реальное время.

## Запуск

    ./.venv/bin/python predict.py tracks/поход.gpx --load 12 --body 75
    ./.venv/bin/python predict.py --wpt "45.98,7.70;45.96,7.68" --load 8 --actual 2.0

Фактическое время берётся из `--actual`, из меток времени в GPX или из
файла-спутника `tracks/поход.json`.

## Что внутри

- `model/dem.py` — высоты из тайлов terrarium (AWS Open Data). Кэш в `cache/`.
  z12 достаточно: на z13-15 те же данные, просто растянутые.
- `model/geo.py` — расстояния, ресемплинг через 25 м, сглаживание профиля,
  набор/сброс с порогом (иначе шум DEM раздувает набор).
- `model/gpx.py` — разбор GPX, время в движении без привалов.
- `model/timemodel.py` — модели времени.
- `route.py` — трек в профиль и сегменты.
- `predict.py` — сравнение всех моделей на одном треке.

## Модели

Классические, как опора: Naismith+Langmuir, DIN 33466, Мюнтер, Тоблер.
Ни одна из них не знает про вес.

Наш кандидат — энергетический. Стоимость метра пути по уклону берём из
полинома Minetti et al. (2002), минимум приходится на уклон около -10%:
пологий спуск дешевле ровного места, что физиологически верно. Дальше
задаём посильную мощность (Вт на кг массы тела) и получаем скорость.
Вес входит в знаменатель физически, а не отдельным штрафом.

Свободные параметры, которые предстоит подобрать по реальным трекам:

- `power_per_kg` — форма человека, сейчас 3.6 Вт/кг
- `load_factor` — во сколько раз кг груза дороже кг своего тела, сейчас 1.15
- `terrain` — множитель за покрытие, сейчас 1.0
- `load_penalty_k` — нелинейная надбавка за тяжёлый рюкзак, сейчас 0

## Что нужно от тебя

Треки в `tracks/`. К каждому — файл `имя.json`:

    {"actual_hours": 5.5, "load_kg": 14, "body_kg": 78}

Обязательны фактическое время в пути и вес рюкзака. Без них трек
для калибровки бесполезен.

---

# Прототип 2 — интерфейс в браузере

    ./.venv/bin/python serve.py

Откроется на `http://localhost:8770`, с телефона — по адресу в локальной сети,
который сервер печатает при запуске. Онбординг заново: добавить `#ob` к адресу.

## Что проверяется

- обводка маршрута пальцем: **один палец рисует, два двигают и зумят карту**
- упрощение линии в экранных пикселях, то есть допуск сам зависит от зума
- плашка сверху с временем и наивной оценкой — сверху, потому что при
  рисовании низ экрана закрыт рукой
- шторка снизу: слайдер веса, цветная шкала нагрузки в процентах от массы тела,
  строка «каждый лишний килограмм», профиль высот, сплиты
- касание графика подсвечивает точку на карте
- три подложки и оверлеи, включая раскраску трека по скорости
- три экрана онбординга

## Устройство

- `docs/index.html` — разметка и стили
- `docs/app.js` — модель, жесты, отрисовка. Модель повторяет Python один в один
- `docs/vendor/` — MapLibre локально, чтобы не зависеть от CDN
- `serve.py` — статика плюс `POST /elev`

Высоты считает сервер, а не браузер: тайлы terrarium отдаются без заголовков
CORS, поэтому прочитать их пиксели из JS нельзя. В нативном приложении такого
ограничения нет — это чисто веб-проблема, не архитектурная.
